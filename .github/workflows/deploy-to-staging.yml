name: Deploy to Staging

on:
  push:
    branches: [testovi]

env:
  STAGING_ENV: staging

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      NODE_VERSION: ${{ secrets.NODE_VERSION }}
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Print custom environment variables
        run: |
          #!/bin/bash
          for var in "${!env}"; do
            if [[ "$var" =~ ^[A-Z_][A-Z0-9_]* ]]; then
              echo "** $var: ${!var}"
            fi
          done
      - name: Install pnpm
        run: npm install -g pnpm
      - uses: actions/cache@v3
        with:
          path: ~/.pnpm
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-
      - run: pnpm install
      - run: pnpm run staging:set
      - run: pnpm run staging

      # - name: Create .staging.env file
      # run: |
      # echo "#!/bin/bash" > create_env.sh
      # echo "for var in \$(env); do" >> create_env.sh
      # echo "  echo \"$var\" >> .staging.env" >> create_env.sh
      # echo "done" >> create_env.sh
      # chmod +x create_env.sh
      # ./create_env.sh
