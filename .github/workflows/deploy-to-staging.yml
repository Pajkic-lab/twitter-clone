name: Deploy to Staging

on:
  push:
    branches: [testovi]

env:
  STAGING_ENV: staging

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      NODE_VERSION: ${{ secrets.NODE_VERSION }}
    steps:
      - uses: actions/checkout@v3

      - uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Insert environment variables into .staging.env
        run: |
          touch .staging.env

          echo NODE_ENV=${{ secrets.NODE_ENV }} >> .staging.env
          echo NODE_VERSION=${{ secrets.NODE_VERSION }} >> .staging.env
          echo PORT=${{ secrets.PORT }} >> .staging.env
          echo BASE_URL_CLIENT=${{ secrets.BASE_URL_CLIENT }} >> .staging.env
          echo BASE_URL_SERVER=${{ secrets.BASE_URL_SERVER }} >> .staging.env
          echo CLOUDINARY_API_KEY=${{ secrets.CLOUDINARY_API_KEY }} >> .staging.env
          echo CLOUDINARY_API_SECRET=${{ secrets.CLOUDINARY_API_SECRET }} >> .staging.env
          echo CLOUDINARY_CLOUD_NAME=${{ secrets.CLOUDINARY_CLOUD_NAME }} >> .staging.env
          echo DATABASE_URL_PG=${{ secrets.DATABASE_URL_PG }} >> .staging.env
          echo GOOGLE_CLIENT_ID=${{ secrets.GOOGLE_CLIENT_ID }} >> .staging.env
          echo GOOGLE_CLIENT_SECRET=${{ secrets.GOOGLE_CLIENT_SECRET }} >> .staging.env
          echo SESSION_NAME=${{ secrets.SESSION_NAME }} >> .staging.env
          echo SESSION_SECRET=${{ secrets.SESSION_SECRET }} >> .staging.env

      - name: Install pnpm
        run: npm install -g pnpm

      - name: set staging
        run: pnpm run staging:set

      - name: run e2e
        run: pnpm run staging
# chmod 600 .staging.env
