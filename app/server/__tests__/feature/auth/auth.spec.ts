describe('auth feature', () => {
  describe('register', () => {
    it('should return session after succesfull registration wia email and password', () => {
      const body = { name: 'John', email: 'john@ymail.com', password: '123', confirmPassword: '123' };
      // expect
    });
  });
});
